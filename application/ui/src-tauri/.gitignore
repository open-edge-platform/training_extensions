# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# PyInstaller backend sidecar (per-host symlinks; see README §"Backend sidecar")
/_internal
/geti-backend-*

# Stale per-host backend output. The sidecar now writes to per-user OS dirs
# (see `apply_default_env` in src/backend.rs); these only appear if someone
# runs the sidecar directly from this folder.
/data/
/logs/
