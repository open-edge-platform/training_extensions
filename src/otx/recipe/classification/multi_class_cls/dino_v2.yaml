model:
  class_path: otx.algo.classification.multiclass_models.vit.VisionTransformerMulticlassCls
  init_args:
    label_info: 1000
    model_name: "dinov2-small"

    optimizer:
      class_path: torch.optim.AdamW
      init_args:
        lr: 0.0005  # Adjusted learning rate
        weight_decay: 0.1  # Increased weight decay
        betas: [0.9, 0.999]
        eps: 1e-8

    scheduler:
      class_path: otx.core.schedulers.LinearWarmupSchedulerCallable
      init_args:
        num_warmup_steps: 5
        main_scheduler_callable:
          class_path: torch.optim.lr_scheduler.CosineAnnealingLR
          init_args:
            T_max: 90  # Total epochs
            eta_min: 1e-6  # Minimum learning rate

engine:
  task: MULTI_CLASS_CLS
  device: auto

callback_monitor: val/accuracy

data: ../../_base_/data/classification.yaml
overrides:
  max_epochs: 90

  callbacks:
    - class_path: otx.algo.callbacks.adaptive_early_stopping.EarlyStoppingWithWarmup
      init_args:
        patience: 7  # Increased patience
        min_delta: 0.01  # Minimum improvement threshold