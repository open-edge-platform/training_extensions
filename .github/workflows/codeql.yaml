name: "CodeQL scan"

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches: ["develop", "releases/**"]
  pull_request:
    branches: ["develop", "releases/**"]

permissions: {}

jobs:
  check_paths:
    name: Check if workflow should run
    runs-on: ubuntu-latest
    if: ${{ github.event_name }} == 'pull_request'
    outputs:
      run_backend: ${{ steps.changed-files-yaml.outputs.backend_any_change && 'false' }}
      run_ui: ${{ steps.changed-files-yaml.outputs.ui_any_change && 'false' }}
      run_actions: ${{ steps.changed-files-yaml.outputs.actions_any_change && 'false' }}
      run_docs: ${{ steps.changed-files-yaml.outputs.docs_any_change && 'false' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          persist-credentials: false

      - name: Get all paths that should trigger the workflow
        id: changed-files-yaml
        uses: tj-actions/changed-files@ed68ef82c095e0d48ec87eccea555d944a631a4c # v46.0.5
        with:
          files_yaml: |
            backend:
              - backend/**
              - lib/**
            ui:
              - ui/**
            actions:
              - .github/**
            docs:
              - docs/**

  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write # required to publish sarif
    needs: check_paths
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
            run: ${{ needs.check_paths.outputs.run_actions || 'true' }}
          - language: python
            build-mode: none
            run: ${{ needs.check_paths.outputs.run_backend || 'true' }}
          - language: javascript-typescript
            build-mode: none
            run: ${{ needs.check_paths.outputs.run_ui || 'true' }}
          - language: rust
            build-mode: none
            run: ${{ needs.check_paths.outputs.run_docs || 'true' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          persist-credentials: false

      # Initializes the CodeQL tools for scanning.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@96f518a34f7a870018057716cc4d7a5c014bd61c # v3.29.10
        if: ${{ matrix.run == 'true' }}
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: security-extended

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@96f518a34f7a870018057716cc4d7a5c014bd61c # v3.29.10
        if: ${{ matrix.run == 'true' }}
        with:
          category: "/language:${{matrix.language}}"
