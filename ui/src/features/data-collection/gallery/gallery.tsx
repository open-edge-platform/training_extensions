import { Text } from '@geti/ui';

export const Gallery = () => {
    return (
        <Text marginTop={'size-2000'} gridArea={'canvas'} UNSAFE_style={{ display: 'block', textAlign: 'center' }}>
            Visualize and manage your media items
        </Text>
    );
};
